int n;

void zaehler(){
   int loop;
   for (loop = 0; loop < 5; loop++){
      n = n - 1;
   }
}

mainAgent {
   n = 10;

   agent a1 = start zaehler();
   agent a2 = start zaehler();
   join(a1);
   join(a2);
   println("Der Wert ist " + n); 
}

